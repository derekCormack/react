import React from "react";
import ReactDOM from "react-dom";
import App from "./App.js";
ReactDOM.render(<App />, document.getElementById("root")); //ReactDOM.render is 
//the function that tells React what to render and where to render it