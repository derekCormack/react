// from index.js **** call React module:    ReactDOM.render(<React.StrictMode><App></App></React.StrictMode>, document.getElementById('root'));
//  to modules 1) react and 2) reactDOM

//index.js  we are rendering the App component, finds the functional or class componetent.   it will render the whole class into the DOM.




function AppNew(props) {
    return (
        <div>This is traditional way of writing ES5 functional component</div>
    )
}